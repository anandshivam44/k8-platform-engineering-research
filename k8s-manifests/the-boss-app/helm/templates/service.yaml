apiVersion: v1
kind: Service
metadata:
  name: {{ include "the-boss-app.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "the-boss-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "the-boss-app.selectorLabels" . | nindent 4 }}
